"use client";

import { useEffect, useMemo, useState } from "react";
import Link from "next/link";
import MobileNav from "../components/MobileNav";
import { supabase } from "../../lib/supabase";

type Gasto = { id: string; nome: string; valor: number; tipo: string; data: string; observacao?: string | null };
const money=(n:number)=>new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"}).format(n);
export default function Gastos(){const [gastos,setGastos]=useState<Gasto[]>([]);const [vendas,setVendas]=useState<number[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState<string|null>(null);async function load(){setLoading(true);setError(null);const [g,s]=await Promise.all([supabase.from("gastos").select("id,nome,valor,tipo,data,observacao").order("data",{ascending:false}).limit(100),supabase.from("vendas").select("valor").limit(500)]);if(g.error||s.error)setError(g.error?.message||s.error?.message||"Erro ao carregar dados");setGastos((g.data??[])as Gasto[]);setVendas((s.data??[]).map(x=>Number(x.valor)));setLoading(false)}useEffect(()=>{load()},[]);const total=useMemo(()=>gastos.reduce((s,g)=>s+Number(g.valor),0),[gastos]);const revenue=useMemo(()=>vendas.reduce((s,v)=>s+v,0),[vendas]);return <main className="min-h-screen bg-slate-50 pb-24 sm:pb-0"><div className="mx-auto max-w-6xl px-4 py-6 sm:px-6 sm:py-8"><Link href="/" className="text-sm text-acai-700 hover:underline">← Início</Link><div className="mt-3 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><h1 className="text-2xl font-bold text-slate-900 sm:text-3xl">Gastos</h1><p className="mt-1 text-sm text-slate-500">Dados reais armazenados no Supabase.</p></div><button onClick={load} disabled={loading} className="w-full rounded-xl bg-acai-700 px-4 py-3 text-sm font-semibold text-white disabled:opacity-60 sm:w-auto">{loading?"Atualizando...":"↻ Atualizar"}</button></div>{error&&<div className="mt-5 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</div>}<div className="mt-5 grid grid-cols-2 gap-3 sm:grid-cols-3 sm:gap-4"><div className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><p className="text-xs text-slate-500">Gastos</p><b className="mt-2 block text-xl">{loading?"...":money(total)}</b></div><div className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm"><p className="text-xs text-slate-500">Faturamento</p><b className="mt-2 block text-xl text-emerald-600">{loading?"...":money(revenue)}</b></div><div className="col-span-2 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm sm:col-span-1"><p className="text-xs text-slate-500">Saldo operacional</p><b className="mt-2 block text-xl text-acai-700">{loading?"...":money(revenue-total)}</b></div></div><section className="mt-4 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm sm:mt-6"><div className="border-b border-slate-100 px-4 py-4 font-semibold sm:px-5">Lançamentos</div>{loading?<div className="p-8 text-center text-sm text-slate-500">Carregando...</div>:gastos.length===0?<div className="p-8 text-center text-sm text-slate-500">Nenhum gasto registrado.</div>:gastos.map(g=><div key={g.id} className="grid grid-cols-[80px_1fr_auto] gap-2 border-b border-slate-100 px-4 py-4 text-sm last:border-0 sm:px-5"><span className="text-slate-400">{new Date(g.data+"T12:00:00").toLocaleDateString("pt-BR")}</span><span className="font-medium">{g.nome}</span><span className="font-semibold text-red-600">- {money(Number(g.valor))}</span></div>)}</section></div><MobileNav/></main>}
